import { createFormAdd } from "./form.add";

describe('Given createFormAdd function', () => {

    afterEach(() => {
        document.body.innerHTML = '';
    });

    describe('', () => {
        //Arrange
        beforeEach(() => {
            createFormAdd();
        });

        test('', () => {
           
        });

        test('', () => {

        });
    });
});